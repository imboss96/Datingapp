import express from 'express';
const router = express.Router();

// Placeholder route to satisfy server import while full implementation is restored
router.get('/', (req, res) => {
  res.json({ ok: true, message: 'lipana route placeholder' });
});

export default router;
